# OSK.Extensions.Petra.Godot

A project that provides many common and useful extensions for a variety Godot related objects. Current extensions include:
 - `FindNode<T>`: finds a node or nodes of a given type within the base node. This can be performed either for a parent or child.
 - `ServiceCollectin`: Adds a node to a dependency container that utilizes standard .NET service collection and providers